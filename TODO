. Make this usable by people other than me
  . "Exclude" is optional
  . Trailing dot is optional
. Webapp
. Choose multiple sets together (direction is ignored if doing multiple sets)
. Split into chunks by cube face instead of fixed chunk size
